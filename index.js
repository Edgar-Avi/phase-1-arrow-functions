const divide = function() {
    const divide = 2000 / 100
    return divide
}
const square = x => x * x

const add = (a , b) => {
    console.log(`Adding ${a}`);
    console.log(`Adding ${b}`);
    return a + b;
}
add(a,b);